use crate::util::grid::Grid;
use crate::util::point::{Point, DOWN, LEFT, RIGHT, UP};
use itertools::Itertools;

#[cfg(test)]
mod tests {
    use super::*;
    use crate::util::grid::Grid;
    use crate::util::point::{Point, DOWN, LEFT, RIGHT, UP};

    #[test]
    fn test() {
        assert!(true)
    }

}
